{
  "name": "product-wiki",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "start": "node ./bin/www",
    "check": "node --check app.js && node --check bin/www && node --check config/database.js && node --check config/passport.js && node --check models/product.js && node --check models/user.js && node --check routes/api.js",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "debug": "^4.4.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.4",
    "morgan": "^1.12.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  }
}
